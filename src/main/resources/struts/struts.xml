<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">
<struts>
	<!-- 指定国际化编码规则 -->
	<constant name="struts.i18n.encoding" value="utf8" />
	<constant name="struts.custom.i18n.resources" value="globalMessages"></constant>
	<constant name="struts.enable.DynamicMethodInvocation" value="true" />
	<!-- 设定最大上传文件的发小为50M -->
	<constant name="struts.multipart.maxSize" value="51200000" />
	<!-- 将Action的创建交给spring来管理 -->  
    <constant name="struts.objectFactory" value="spring" />  
	
	<package name="abstract-struts" abstract="true" namespace="/" extends="struts-default">
		
		<interceptors>
			<interceptor name="timeout" class="com.purplelight.mcm.iterceptor.SessionIterceptor"></interceptor>
			<interceptor-stack name="mydefault">
				<interceptor-ref name="defaultStack"></interceptor-ref>
				<interceptor-ref name="timeout"></interceptor-ref>
			</interceptor-stack>
		</interceptors>
	
		<global-results>
			<result name="error">/common/error.jsp</result>
			<result name="input">/common/input.jsp</result>
			<result name="login">/common/timeout.jsp</result>
		</global-results>
	</package>
	
	<include file="/struts/struts-user.xml"></include>
	
</struts>